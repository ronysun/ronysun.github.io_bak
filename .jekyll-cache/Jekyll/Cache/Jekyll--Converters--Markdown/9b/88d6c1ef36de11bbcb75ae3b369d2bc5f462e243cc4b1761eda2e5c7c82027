I"<h2 id="git-status引起的命令">git status引起的命令</h2>
<h3 id="git-add">git add</h3>
<p>使用git status命令可以看到被修改的文件状态为modified，新增文件未Untracked，此时
可以通过git add <文件名>进行commit前的存储(staged)。同时，可通过git checkou -- <文件名>放弃修改。</文件名></文件名></p>
<h3 id="git-reset-head">git reset HEAD</h3>
<p>通过git add命令添加到暂存区的修改，可以需要通过git reset从暂存区撤回到工作区</p>
:ET